{
  "name": "easyproxy-worker",
  "private": true,
  "version": "0.8.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260922.0",
    "typescript": "^5.9.0",
    "wrangler": "^4.34.0"
  }
}
